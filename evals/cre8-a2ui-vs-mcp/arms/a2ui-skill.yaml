# Skill arm: the cre8-a2ui skill's markdown, copied into the agent's skills dir.
# `prepare.sh` materialises .arm-inputs/cre8-a2ui first.
# See _shared.md before editing. Run: harbor run -c arms/a2ui-skill.yaml
job_name: cre8-a2ui-vs-mcp__a2ui-skill
jobs_dir: jobs
n_attempts: 3
n_concurrent_trials: 4

datasets:
  - path: tasks
    # mcp-freecode-portfolio is scored by building and rendering real React
    # code, which needs Bash and an installed @tmorrow/cre8-react - neither of
    # which this arm's isolation allows. It has its own *-freecode.yaml arm
    # per condition, which relaxes that isolation deliberately; see the note
    # in arms/baseline-freecode.yaml.
    exclude_task_names:
      - mcp-freecode-portfolio

agents:
  - name: claude-code
    model_name: claude-opus-5
    kwargs:
      # The arm under test must be the agent's only route to the catalog.
      # Without this the agent simply npm-packs @tmorrow/cre8-wc and reads
      # catalog.json, which makes all three arms the same experiment.
      # Identical in every arm, so it constrains the comparison equally.
      disallowed_tools: Bash,WebFetch,WebSearch,Task
    skills:
      - .arm-inputs/cre8-a2ui
