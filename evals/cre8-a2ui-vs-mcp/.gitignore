jobs/
# Harbor run output, kept aside when re-running against a new library
# version. Same content as jobs/, just not the current run - 263M of agent
# transcripts and built artifacts, regenerated by re-running the arms.
jobs-archive-*/
.arm-inputs/
tasks/*/environment/ca/*.crt
gallery.html
compare.html
jobs-demo/
