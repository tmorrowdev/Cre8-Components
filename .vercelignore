# The deployment builds the cre8-wc Storybook (packages/cre8-wc/dist) — see
# vercel.json buildCommand/outputDirectory. Keep the monorepo source, lockfile,
# and custom-elements.json (build inputs); exclude only large, unrelated dirs.
node_modules
.nx
.git
.claude
secure-agent-team
graphify-out
docs
memory
website
*.pen
